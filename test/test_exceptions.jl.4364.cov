        - # Test suite for exception handling system.
        - # Tests custom exception types and error handling workflows.
        1 function test_exceptions(client) 
        1     @testset "Error Message Quality" begin
        -         # Basic test that error handling works without exposing internal types
        2         clock = CachedEpochClock(EpochClock())
        1         properties = TestAgent.Properties(clock)
        1         comms = CommunicationResources(client, properties)
        1         base_agent = BaseRtcAgent(comms, properties, clock)
        1         agent = TestAgent.RtcAgent(base_agent)
        -         
        -         # This should throw an informative error
        1         try
        1             start(agent)  # Invalid state transition
        1             @test false  # Should have thrown an exception
        -         catch e
        1             @test e isa Exception
        1             @test !isempty(string(e))  # Error message should not be empty
        -         end
        -         
        1         Agent.on_close(agent)
        -     end
        - end
