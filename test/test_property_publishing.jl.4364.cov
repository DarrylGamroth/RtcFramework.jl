        - """
        - Test suite for property publishing system.
        - Tests the integration of strategies with actual property publishing.
        - """
        1 function test_property_publishing(client)
        1     @testset "Property Publication Workflow" begin
        3         clock = CachedEpochClock(EpochClock())
        1         properties = TestAgent.Properties(clock)
        1         comms = CommunicationResources(client, properties)
        1         base_agent = BaseRtcAgent(comms, properties, clock)
        1         agent = TestAgent.RtcAgent(base_agent)
        1         Agent.on_start(agent)
        -         
        -         # Test that properties are accessible
        1         @test base(agent).properties !== nothing
        -         
        -         # Test that registry is accessible
        1         @test base(agent).publication_configs isa Vector
        -         
        1         Agent.on_close(agent)
        -     end
        -     
        -     @testset "Periodic Publishing" begin
        3         clock = CachedEpochClock(EpochClock())
        1         properties = TestAgent.Properties(clock)
        1         comms = CommunicationResources(client, properties)
        1         base_agent = BaseRtcAgent(comms, properties, clock)
        1         agent = TestAgent.RtcAgent(base_agent)
        1         Agent.on_start(agent)
        -         
        -         # Test basic agent functionality
        1         @test base(agent).properties !== nothing
        -         
        1         Agent.on_close(agent)
        -     end
        -     
        -     @testset "Multiple Strategy Integration" begin
        3         clock = CachedEpochClock(EpochClock())
        1         properties = TestAgent.Properties(clock)
        1         comms = CommunicationResources(client, properties)
        1         base_agent = BaseRtcAgent(comms, properties, clock)
        1         agent = TestAgent.RtcAgent(base_agent)
        1         Agent.on_start(agent)
        -         
        -         # Test basic agent functionality
        1         @test base(agent).properties !== nothing
        -         
        1         Agent.on_close(agent)
        -     end
        -     
        -     @testset "Publication Config Management" begin
        3         clock = CachedEpochClock(EpochClock())
        1         properties = TestAgent.Properties(clock)
        1         comms = CommunicationResources(client, properties)
        1         base_agent = BaseRtcAgent(comms, properties, clock)
        1         agent = TestAgent.RtcAgent(base_agent)
        -         
        -         # Test basic agent functionality without streams
        1         @test base(agent).publication_configs isa Vector
        1         @test isempty(base(agent).publication_configs)  # No registrations yet
        -         
        1         Agent.on_start(agent)
        1         Agent.on_close(agent)
        -     end
        -     
        -     @testset "Strategy State Updates" begin
        3         clock = CachedEpochClock(EpochClock())
        1         properties = TestAgent.Properties(clock)
        1         comms = CommunicationResources(client, properties)
        1         base_agent = BaseRtcAgent(comms, properties, clock)
        1         agent = TestAgent.RtcAgent(base_agent)
        1         Agent.on_start(agent)
        -         
        -         # Test basic agent functionality
        1         @test base(agent).properties !== nothing
        -         
        1         Agent.on_close(agent)
        -     end
        -     
        -     @testset "Property Access in Publishing" begin
        -         # Test basic property access without stream dependencies
        3         periodic_strategy = Periodic(1000)
        1         @test RtcFramework.interval_ns(periodic_strategy) == 1000
        -         
        1         onupdate_strategy = OnUpdate()
        1         @test onupdate_strategy isa PublishStrategy
        -         
        -         # Test should_publish functions work
        1         @test RtcFramework.should_publish(onupdate_strategy, 100, -1, 200, 1500) isa Bool
        -     end
        -     
        -     @testset "Zero Allocation Publishing" begin
        2         clock = CachedEpochClock(EpochClock())
        1         properties = TestAgent.Properties(clock)
        1         comms = CommunicationResources(client, properties)
        1         base_agent = BaseRtcAgent(comms, properties, clock)
        1         agent = TestAgent.RtcAgent(base_agent)
        1         Agent.on_start(agent)
        -         
        -         # Test basic agent functionality
        1         @test base(agent).properties !== nothing
        -         
        1         Agent.on_close(agent)
        -     end
        - end
